apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-db-customer
spec:
  serviceName: postgres-customer-clusterip-srv
  replicas: 1
  selector:
    matchLabels:
      app: postgres-db-customer
  template:
    metadata:
      labels:
        app: postgres-db-customer
    spec:
      containers:
        - name: postgres-db-customer
          image: postgres:10.1
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: customer
            - name: POSTGRES_USER
              value: customer
            - name: POSTGRES_PASSWORD
              value: customer
            - name: PGDATA
              value: /var/lib/postgresql/data/customer
          volumeMounts:
            - name: postgres-customer-pv-storage
              mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-customer-pv-storage
        persistentVolumeClaim:
          claimName: postgres-customer-pv-claim
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-customer-clusterip-srv
spec:
  type: ClusterIP
  selector:
    app: postgres-db-customer
  ports:
  - name: postgres-db-customer
    protocol: TCP
    port: 5432
    targetPort: 5432 
