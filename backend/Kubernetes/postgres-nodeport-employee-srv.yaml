apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport-employee-srv
spec:
  type: NodePort
  selector:
    app: postgres-db-employee
  ports:
    - name: postgres-db-employee
      protocol: TCP
      port: 5432
      targetPort: 5432