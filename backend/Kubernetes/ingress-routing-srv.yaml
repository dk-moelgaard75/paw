apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-srv
spec:
  ingressClassName: nginx   
  rules:
    - host: acme.com
      http:
        paths:
          - path: /api/employee
            pathType: Prefix
            backend:
              service:
                name: employee-clusterip-srv
                port:
                  number: 80
                  

    